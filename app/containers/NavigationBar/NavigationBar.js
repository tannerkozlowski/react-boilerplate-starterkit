import React, { Component } from 'react';

import './style.scss';

class NavigationBar extends Component {
  render() {
    return (
      <nav className="nav">
        Navbar
      </nav>
    );
  }
}

export default NavigationBar;
